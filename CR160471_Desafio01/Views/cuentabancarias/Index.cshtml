@model IEnumerable<CR160471_Desafio01.Models.cuentabancaria>

@{
    ViewBag.Title = "Cuentas bancarias";
}

<h2>Cuentas bancarias</h2>

<p>
    @Html.ActionLink("Agregar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cliente.nombres)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipocuentabancaria.tipocuenta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.modena)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cliente.nombres)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipocuentabancaria.tipocuenta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.modena)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id }) |
            @Html.ActionLink("Ver", "Details", new { id=item.id }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
